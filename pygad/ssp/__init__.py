'''
Module for SSP-models.

Also doctest other parts of this sub-module:
    >>> import doctest
    >>> doctest.testmod(SSP_old)
    TestResults(failed=0, attempted=12)
    >>> doctest.testmod(bc_ssp)
    TestResults(failed=0, attempted=9)
'''
from SSP_old import *
from bc_ssp import *

